services:
  - type: web
    name: bridgeapi
    runtime: docker
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: PORT
        value: "10000"
    plan: free
    autoDeploy: true
